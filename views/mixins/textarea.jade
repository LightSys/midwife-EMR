//- Auto expanding text area.
//- Adapted from [Expanding Text Areas Made Elegant](http://alistapart.com/article/expanding-text-areas-made-elegant).
//- Assumes that supporting CSS and JS is in place.
mixin textarea(id, name, classes, tabindex, value, isDisabled)
  div(class='expandingArea')
    pre
      span
      br
    textarea(class=classes, name=name, tabindex=tabindex, disabled=isDisabled)= value
