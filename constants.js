/* 
 * -------------------------------------------------------------------------------
 * constants.js
 *
 * A place to define constant values that every module can get to without
 * dependency issues.
 * ------------------------------------------------------------------------------- 
 */

const
  KEY_VALUE_UPDATE = 'key_value_update'
  ;

module.exports = {
  KEY_VALUE_UPDATE
};
